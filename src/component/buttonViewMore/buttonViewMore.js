import React from 'react';
import './styled.css';

const ButtonViewMore = ({ label }) => {
    return (
        <button className="buttonViewMore"> {label} </button>
    )
}

export default ButtonViewMore;